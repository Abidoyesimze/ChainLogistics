# ChainLogistics Java SDK

Java SDK for the ChainLogistics API with a client surface aligned to the Python and Rust SDKs.

## Included operations

- `health()`
- `readiness()`
- `liveness()`
- `monitoringHealth()`
- `dbHealth()`
- `listProducts()`

## Build and test

```bash
mvn test
```
